# macOS / editor noise
.DS_Store
.Rhistory
.Rproj.user/
.Rapp.history
.idea/
.vscode/
Thumbs.db

# Python / notebooks
__pycache__/
*.pyc
.ipynb_checkpoints/

# Heavy intermediate artifacts (kept locally, not pushed)
transcription-scripts/transcription_chunks/
transcription-scripts/transcription_chunks_v2/
transcription-scripts/retranscribe_chunks_v3/
transcription-scripts/retranscribe_feb27_chunks/

# CSVs/scripts/transcription-scripts source files ARE committed
# (build.py + the classification CSV are needed to rebuild data/)

# Credentials and session state — never commit
.env
.env.*
*.session
*.session-journal

# Raw LLM response caches (regenerable, ~14MB)
cache/
cache_lego/

# Backups and duplicated corpora
*.bak
notebooks/explosive_media_messages.csv
notebooks/golden_dataset_extra.csv
